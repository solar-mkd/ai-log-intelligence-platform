# secrets / local config
config/*.yaml
!config/*.example.yaml
.env

# python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/

# ML / model caches
.cache/

# os
.DS_Store
Thumbs.db
desktop.ini

# bulk raw data — ignored recursively, but keep the folder present
data/raw/**
!data/raw/.gitkeep

# editor (keep the shared VS Code settings)
.vscode/*
!.vscode/settings.json

helper.txt